This project uses a small rotating User-Agent pool for Yahoo Finance (yfinance) requests.

Purpose: reduce trivial throttling fingerprinting. This is best-effort only.
Real rate limiting can be based on IP / request patterns.
